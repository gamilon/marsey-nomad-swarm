# Example only (user lab / password change-me-edge). Copy to htpasswd or regenerate:
#   docker run --rm httpd:2.4-alpine htpasswd -nbB lab 'your-password' > nginx/htpasswd
lab:$apr1$Iyi4sy3U$THRGqtp6Y3J.ltSZuU2On1
